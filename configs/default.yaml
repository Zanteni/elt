seed: 42
device: cuda

data:
  root: /kaggle/input/cifar10
  dataset: cifar10
  batch_size: 128
  num_workers: 4

optimizer:
  name: adamw
  lr: 3e-4
  weight_decay: 0.0

train:
  max_steps: 200000
  log_every: 100
  eval_every: 2000
  ckpt_every: 5000
  ema_decay: 0.9999